desc "Commit files and deploy site to fdurant.github.io"
task :deploy do
  system "git add --all"
  system "git commit -a -m 'Updating statically generated files'"
  system "git push origin master"
end
